テーブル定義

CREATE TABLE EMPLOYEE(
E_ID INT AUTO_INCREMENT PRIMARY KEY,
NAME VARCHAR(100) NOT NULL,
AGE INT NOT NULL,
SEX VARCHAR(50) NOT NULL,
J_DATE VARCHAR(100) NOT NULL,
L_DATE VARCHAR(100) NOT NULL
);



CREATE TABLE ID_TABLE(
E_ID INT PRIMARY KEY,
D_ID INT NOT NULL,
I_ID INT NOT NULL,
A_ID INT NOT NULL
);

CREATE TABLE ADDRESS(
A_ID INT AUTO_INCREMENT PRIMARY KEY,
POSTCODE VARCHAR(50) NOT NULL,
PREF VARCHAR(100) NOT NULL,
CITY VARCHAR(100) NOT NULL
);

CREATE TABLE DEPARTMENT(
D_ID INT AUTO_INCREMENT PRIMARY KEY,
D_NAME VARCHAR(50) UNIQUE NOT NULL
);

CREATE TABLE IMAGE(
I_ID INT AUTO_INCREMENT PRIMARY KEY,
CONTENT_TYPE VARCHAR(50) NOT NULL,
IMG BLOB NOT NULL
);

インサート
INSERT INTO EMPLOYEE(NAME,AGE,SEX,J_DATE,L_DATE) 
VALUES('名前',24,'MAN','2000/01/01','2000/01/01');

INSERT INTO ID_TABLE(E_ID,D_ID,I_ID,A_ID) 
VALUES(1,1,1,1);

INSERT INTO
ADDRESS(POSTCODE,PREF,CITY) 
VALUES
('郵便番号','都道府県','市町村');

INSERT INTO
IMAGE(PATH) 
VALUES
('PATH');

セレクト
SELECT I_ID FROM IMAGE WHERE PATH = 'PATH';

SELECT LAST_INSERT_ID() AS LAST;

SELECT * FROM EMPLOYEE JOIN ID_TABLE ON EMPLOYEE.E_ID = ID_TABLE.E_ID WHERE NAME LIKE '名前' AND EMPLOYEE.E_ID LIKE 1 AND D_ID LIKE 1;

SELECT * FROM EMPLOYEE JOIN ID_TABLE ON EMPLOYEE.E_ID = ID_TABLE.E_ID WHERE D_ID = 1;


デリート
DROP TABLE EMPLOYEE;